import React from "react";
import '../styles/App.css';

const Scan: React.FC = () => {
    return (
        <div className="home">
            <h1>Scan</h1>
        </div>
    );
};

export default Scan;
